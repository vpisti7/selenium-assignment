# Selenium Assignment

For the full assignment description, task list, and deadlines, see the course materials.

Fill in `points.yml` to track your progress. The CI will calculate your grade on every push.

Instruction:
I copied the docker-compose.yml file into the GitHub repo so it would be visible what changes I made in order to test it in both Chrome and Firefox.